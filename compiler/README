
JSON MATERIALISED OPERATORS VERSION
--------------------

    Python version 2.6.6 was used in development and testing (default on hpdc02).

RUNNING 
2 tasks(nodes) - tfidf followed by kmeans

    python jsontocpp.py -i tfidf_then_kmeans.json -l QUB_materialized.json
Where:
    tfidf_then_kmeans.json is a workflow for tfidf followed by kmeans (separate node tasks)
    based on Unige's workflow output after optimisation which makes every task a node.
    
RUNNING 
1 in-memory tasks(nodes) - tfidf and kmeans in one executable

    python jsontocpp.py -i tfidf_and_kmeans.json -l QUB_materialized.json
Where:
    tfidf_and_kmeans.json is a workflow for tfidfkmeans operator all in-memory



OLDER VERSION BEFORE INTEGRATION CHANGES:

RUNNING PRIOR TO INTEGRATION CHANGES, IGNORE
    python jsontocpp.py -i tfidf_list.json -l QUB_materialized.json -o tfidflist.cpp
    python jsontocpp.py -i tfidf_map.json -l QUB_materialized.json -o tfidfmap.cpp
    python jsontocpp.py -i kmeans.json  -l QUB_materialized.json -o kmeans.cpp
    python jsontocpp.py -i tfidfkmeans.json -l QUB_materialized.json -o tfidf_kmeans.cpp

Older version Where:
    tfidf.json is a simple 'imagined' of the workflow json generated by Unige
    QUB_materialized.json is a library holding materialised operator data
    tmplist.cpp is the generated CPP.

